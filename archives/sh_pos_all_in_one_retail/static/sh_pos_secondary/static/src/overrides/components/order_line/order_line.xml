<?xml version="1.0" encoding="UTF-8"?>
<templates id="sh_product_reference_template" xml:space="preserve">

    <t t-name="Orderline" t-inherit="point_of_sale.Orderline" t-inherit-mode="extension">
        <!-- <xpath expr="//ul[hasclass('info-list')]" position='attributes'>
            <attribute name="t-if">props.line.get_current_uom == props.line.unit</attribute>
        </xpath>
        <xpath expr="li[hasclass('orderline')]" position="inside">
            <ul class="info-list sh_info_list" t-if='(props.line.secondary_unit_name !== props.line.unit)' >
                <li class="price-per-unit">
                    <em t-esc="line.qty" class="qty fst-normal fw-bolder me-1" /> 
                    <t t-if="props.line.secondary_unit_name"> 
                        <span> <t t-esc="props.line.secondary_unit_name" /> </span>
                    </t>
                    <t t-else=""> 
                        <t t-if="line.unit" t-esc="line.unit" />
                    </t>
                    x <s t-esc="line.oldUnitPrice" t-if="line.oldUnitPrice" /> 
                    <t t-if="line.secondary_unit_price and (line.display_secondary_cart or line.display_secondary_receipt_changes)" >
                        <t t-esc="line.secondary_unit_price" /> <span t-if="line.get_current_uom"> / <t t-esc="line.get_current_uom" /></span>
                    </t>
                    <t t-else="" >
                        <t t-esc="line.unitPrice" /> <span t-if="line.unit"> / <t t-esc="line.unit" /></span>
                    </t>
                </li>
                <li t-if="line.discount and line.discount !== '0'">
                    With a <em><t t-esc="line.discount" />% </em> discount
                </li>
                <t t-slot="default" />
                <li t-if="line.customerNote" class="customer-note w-100 p-2 my-1 rounded text-break text-bg-warning text-warning bg-opacity-25">
                    <i class="fa fa-sticky-note me-1" role="img" aria-label="Customer Note" title="Customer Note"/>
                    <t t-esc="line.customerNote" />
                </li>
                <li t-if="line.internalNote" class="internal-note w-100 p-2 my-1 rounded text-bg-info text-info bg-opacity-25 text-break">
                    <i class="fa fa-tag me-1" role="img" aria-label="Note" title="Note"/>
                    <t t-esc="line.internalNote" />
                </li>
            </ul>
        </xpath> -->
    </t>
</templates>
